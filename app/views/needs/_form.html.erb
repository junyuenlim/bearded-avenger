<%= form_for([@project, @need]) do |f| %>
	<% if @need.errors.any? %>
	  <div id="error_explanation">
	    <h2><%= pluralize(@need.errors.count, "error") %> prohibited this need from being saved:</h2>

	    <ul>
	    <% @need.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
	<% end %>

	<div class="form-group">
		<%= f.text_field :title, class: "form-control", placeholder: "We need..." %>
	</div>

	<div class="form-group">
		<%= f.text_field :current, class: "form-control", placeholder: "We need..." %>
	</div>

	<div class="form-group">
		<%= f.text_field :goal, class: "form-control", placeholder: "We need..." %>
	</div>

	<%= f.submit class: "btn btn-primary" %>
<% end %> 