<%= form_for(current_user.relationships.find_by_followedproject_id(@project),
         html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow Project", class: "btn btn-large" %>
<% end %>