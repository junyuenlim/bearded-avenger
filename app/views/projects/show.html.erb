<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @project.title %>
</p>

<p>
  <b>Description:</b>
  <%= @project.description.html_safe %>
</p>

<div class="panel-footer">
	<h3 class="stat"><%= @project.followers.count %> / 10  </h3>
    <p>followers</p>
    <div class="progress">
      <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="<%= number_with_precision(((@project.followers.count.to_f / 10) * 100), :precision => 0) %>" aria-valuemin="0" aria-valuemax="100" style="width: <%= number_to_percentage((@project.followers.count.to_f / 5 * 100), :precision => 2) %>;">
      </div>
    </div>

	<% @user ||= current_user %>
	<div class="stats text-center">
	  <a href="<%= followers_project_path(@project) %>">
	  </a>
	  <% unless user_signed_in? %>
	    <%= link_to "Log In to Follow Project", new_user_session_path, class: "btn btn-primary" %>
	  <% end %>
	</div>
	<%= render 'follow_form' if signed_in? %>
	  <% if current_user == @project.user %>
	   You are Project Owner
	  <% end %>
</div>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
