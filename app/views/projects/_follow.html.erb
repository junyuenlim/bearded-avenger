<%= form_for(current_user.relationships.build(followedproject_id: @project.id), remote: true) do |f| %>
  <div><%= f.hidden_field :followedproject_id %></div>
  <%= f.submit "Follow Project", class: "btn btn-large btn-primary" %>
<% end %>